import { Fraunces, Barlow } from "next/font/google";

export const titleFont = Fraunces({ subsets: ["latin"], weight: ["900"] });
export const bodyFont = Barlow({ subsets: ["latin"], weight: ["400"] });
